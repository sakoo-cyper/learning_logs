# learning_logs
learning logs is a simple Django project from the learning python crash course book.

but all files and folder inside a folder name "learning_log"
install the requirements.txt file using pip -r install requirements.txt
